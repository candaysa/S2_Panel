*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
# Symlink to a local AI-tooling cache outside the project (~/.omo/codegraph/...)
# - machine-specific, not part of the app, never safe to commit.
/.codegraph
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
# AI-agent working directory (session/plan/draft state). Not part of the
# application; not needed to build or run it. Keeping it out of the repo
# by default is the safer choice even though a scan found nothing sensitive
# in it at the time this line was added.
/.omo
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
# Local dev sqlite DB - generated by `composer setup` / post-create-project.
# Never commit a database that might later hold real test data.
/database/database.sqlite
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
